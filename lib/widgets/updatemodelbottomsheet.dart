import 'package:flutter/material.dart';

class updateModalBottomSheet extends StatelessWidget {
  const updateModalBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
